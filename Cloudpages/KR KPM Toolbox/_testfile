const extractCellDeepLinks = (keyword) => {
  let results = [];

  for (const cellAddress in sheet) {
    if (sheet[cellAddress]?.v === keyword) {
      const columnLetter = cellAddress.match(/[A-Z]+/)[0]; // Extract column letter
      let rowNumber = parseInt(cellAddress.match(/\d+/)[0]); // Extract row number

      // Loop through rows below until an empty cell is encountered
      while (true) {
        rowNumber++; // Move to the next row
        let nextCellAddress = columnLetter + rowNumber;

        if (!sheet[nextCellAddress]?.v) break; // Stop if the cell is empty

        results.push(sheet[nextCellAddress].v);
      }
      break; // Stop after finding the first occurrence of keyword
    }
  }

  return results.length > 0 ? results.join(", ") : "No valid data found";
};

// Example usage:
document.getElementById("test1").value = extractCellDeepLinks("Deep Link");